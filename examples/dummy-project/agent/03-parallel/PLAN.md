# Plan: independent greeting modules (parallel-friendly)

Each task is independent, so they can run concurrently in their own worktrees.

- [ ] Create hello_en.py that prints an English greeting
- [ ] Create hello_es.py that prints a Spanish greeting
- [ ] Create hello_fr.py that prints a French greeting
- [ ] Create hello_de.py that prints a German greeting
