# Plan: gather the Cohere Chat API reference

A single task that pulls the reference material a later stage will build on. It
fetches whatever it needs itself, so it is self-contained.

- [ ] Fetch the Cohere Chat API reference from https://docs.cohere.com/reference/chat and save the relevant request/response details to cohere-chat-reference.md
