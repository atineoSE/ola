# Plan: record today's date

Each task writes its own file, so they are independent and parallel-safe — the
ordering this folder cares about is that it finishes before `02-get-docs` starts.

- [ ] Create date.txt containing today's date in YYYY-MM-DD form
- [ ] Create summary.txt with a one-line description of this demo project
