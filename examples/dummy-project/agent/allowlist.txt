# Project-specific domains opened through the sandbox's deny-by-default network
# policy. One host per line; subdomains are included automatically. Blank lines
# and lines starting with '#' are ignored. See docs/sandbox.md.

# 02-utils fetches the Cohere Chat API reference (docs.cohere.com) and calls
# the Chat API itself (api.cohere.com); without a key the call blocks → janitor.
docs.cohere.com
api.cohere.com
