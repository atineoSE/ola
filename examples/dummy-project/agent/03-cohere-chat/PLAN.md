# Plan: send a message through the Cohere Chat API

A single task that builds on the reference captured in `02-get-docs/`. It uses
the request/response details saved there to make one real call.

- [ ] Using the Cohere Chat API and the request details in 02-get-docs/cohere-chat-reference.md, send a single "hi" message and save the model's reply to cohere-hello.txt
